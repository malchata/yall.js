{
  "name": "yall",
  "version": "2.0.0",
  "description": "Yet Another Lazy Loader",
  "main": "./src/yall.js",
  "scripts": {
    "clean": "rimraf ./dist && mkdir -p ./dist",
    "build": "npm run clean && npx babel $(node -p -e \"require('./package.json').main\") | npx uglifyjs --timings --warn --verbose --mangle --output ./dist/yall-$(node -p -e \"require('./package.json').version\").min.js && npm run update-test",
    "update-test": "cp -fv ./dist/yall-$(node -p -e \"require('./package.json').version\").min.js ./test/js/yall.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/malchata/yall.js.git"
  },
  "keywords": [
    "lazy load",
    "images",
    "web performance"
  ],
  "author": "Jeremy L. Wagner <jeremy.l.wagner@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/malchata/yall.js/issues"
  },
  "homepage": "https://github.com/malchata/yall.js#readme",
  "devDependencies": {
    "express": "^4.16.3",
    "npx": "^10.0.1",
    "uglify-js": "^3.3.20",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "rimraf": "^2.6.2"
  }
}
